from agents.halfin import prerequisite_checker

if __name__ == '__main__':
    directory = '.'  # Replace with the directory of your Python files
    prerequisite_checker.check_for_prerequisites(directory)